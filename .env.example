# ============================================================
# SGTI Municipal — Variables de entorno DEMO
# Copia este archivo como .env y cambia solo JWT_SECRET
# ============================================================

# Entorno
NODE_ENV=development
PORT=3001

# JWT — pon cualquier texto largo aqui, no importa cual
JWT_SECRET=cambia_esto_por_cualquier_texto_largo

JWT_EXPIRES_IN=24h

# Base de datos demo — Supabase (no necesitas instalar nada)
DB_DIALECT=postgres
DB_SSL=true
DB_UNIFIED_HOST=db.ofcszeovccsolbalsuux.supabase.co
DB_UNIFIED_PORT=5432
DB_UNIFIED_NAME=postgres
DB_UNIFIED_USER=postgres
DB_UNIFIED_PASS=Lynx789123.$

# Desactivado en entorno demo
DISABLE_WHATSAPP=true
OPENAI_API_KEY=
